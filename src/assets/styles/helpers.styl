centralize(type)
  if type == block
    margin-right: auto
    margin-left: @margin-right
  else if type == absolute
    position: absolute
    top: 50%
    left: 50%
    transform: translateX(-50%) translateY(-50%)
